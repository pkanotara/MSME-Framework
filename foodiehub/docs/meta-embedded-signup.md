# Meta Embedded Signup Notes

FoodieHub only automates setup steps allowed after a merchant explicitly completes Meta Embedded Signup.

- The user must authenticate with Meta and connect their own WhatsApp Business assets.
- The backend consumes callback data (WABA ID, phone number ID, code) and exchanges code for token.
- The platform does not create WhatsApp Business Accounts programmatically.
