import { PageShell } from '@/components/PageShell';

export default function RestaurantsPage() {
  return <PageShell title="Restaurants">Manage all restaurant records and onboarding status.</PageShell>;
}
